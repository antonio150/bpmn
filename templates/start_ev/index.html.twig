{% extends 'base.html.twig' %}

{% block title %}Hello StartEvController!{% endblock %}



{% block body %}

{# {% set content = include("groupe_org/index.html.twig") %}  #}


<div class="example-wrapper">
    {# <h1>Hello {{ controller_name }}! ✅</h1> #}
  {# {{ render( controller('App\\Controller\\GroupeOrgController::index') ) }} #}

    {{ form_start(form) }}
   

    <div class="form-group">
        {{ form_label(form.nameStart) }}
        {{ form_widget(form.nameStart) }}
        <div class="form-error">
            {{ form_errors(form.nameStart) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.typeStart) }}
        {{ form_widget(form.typeStart) }}
        <div class="form-error">
            {{ form_errors(form.typeStart) }}
        </div>
    </div>

    
  
    

    <div class="form-group">
        {{ form_row(form.submit) }}
    </div>

   
{{ form_end(form) }}

</div>
{% endblock %}
